# Simple Notes App (Vue 3 + Vite)

Run locally:
- npm install
- npm run dev

The app uses localStorage only; no backend required. Keyboard shortcuts:
- Ctrl/Cmd + N: New note
- Ctrl/Cmd + S: Save
- Delete/Backspace: Delete selected (with confirm)
